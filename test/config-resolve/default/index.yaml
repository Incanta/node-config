hello: world
goodbye: ${hello}
foo: ${hello}-hello
bar: ${foo}-bar
multi: ${foo}-${bar}
formatted:
  format: "${hi} ${bye}"
  hi: hello
  bye: world
format-ref: ${formatted}
root:
  child: ${hello}-child
  child2:
    child3: ${hello}-child3